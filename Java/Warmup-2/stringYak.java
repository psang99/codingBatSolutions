public String stringYak(String str) {return str.replace("yak","");/*  String t="";  String m="";    for(int i=0;i<=str.length()-3;i++)   {    if((str.charAt(i)=='y')&&(str.charAt(i+1)=='a')&&(str.charAt(i+2)=='k'))    {     t=str.substring(0,i)+str.substring(i+3,str.length());     // break;    }    else    m=m+str.charAt(i);   }if(str.contains("yak"))  return t;  else   return m;*/}